{% extends 'base.html.twig' %}

{% block title %}Modifier un Lieu{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier un Lieu</h1>
        <div class="col-md-6">
            {{ include('lieu/_formulaire.html.twig', {'button_label': 'Modifier'}) }}
        </div>
        <br>
        <a class="btn btn-dark" href="{{ path('app_lieu_index') }}">Retour liste</a>
        <br>
        {{ include('lieu/_formulaire_suppression.html.twig') }}
    </div>
{% endblock %}